{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The `cloudflare:test` module declaration used by the API tests.
    "types": ["@cloudflare/vitest-pool-workers/types"],
    "strictNullChecks": true
  },
  "include": ["worker-configuration.d.ts", "src/**/*.ts", "test/**/*.ts"]
}
